server.port = 8080
server.error.include-message = always

# H2 in-memory database settings
# Enabling the H2-Console (local and remote)
#spring.h2.console.enabled=true
#spring.h2.console.settings.web-allow-others=true
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.show-sql = true
# Data source production
# Postgres
spring.datasource.url = ${{ secrets.GCP_DB_URL }}
spring.datasource.username = ${{ secrets.GCP_DB_USERNAME }}
spring.datasource.password = ${{ secrets.GCP_DB_PASSWORD }}
spring.datasource.driver-class-name = org.postgresql.Driver
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQLDialect

# data persistance options on each build
spring.jpa.hibernate.ddl-auto = update
spring.datasource.initialization-mode = always
spring.devtools.add-properties = true